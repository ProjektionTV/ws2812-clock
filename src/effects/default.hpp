#ifndef _DEFAULT_HPP_
#define _DEFAULT_HPP_

#include "main.hpp"

namespace Effects {
    namespace Default {
        uint8_t addMidd();
        uint8_t addRing();
        uint8_t addColor();
        uint8_t addTransition();
        void init();
    } // namespace Default
} // namespace Effects


#endif /* _DEFAULT_HPP_ */
